# Copy to .env and fill in. Credentials are read at call time only — they must never
# appear in configuration files, source code or committed files.
OPENAI_API_KEY=sk-your-key-here
# Optional: any OpenAI-compatible endpoint (proxy, Ollama, vLLM, ...)
OPENAI_BASE_URL=https://api.openai.com/v1
